defmodule Customer.Api.V1.FavoriteJobView do
  use Customer.Web, :view

  def render("index.json", %{jobs: jobs}) do

  end

  def render("create.json", %{job_id: job_id}) do

  end

  def render("update.json", %{job_id: job_id}) do

  end

  def render("delete.json", %{job_id: job_id}) do
     
  end
end
